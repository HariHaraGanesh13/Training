<?php
/**
 * Copyright © Magento, Inc. All rights reserved.
 * See COPYING.txt for license details.
 */
/** @var \Magento\Framework\View\Element\Template $block */
$id = (int)$block->getRequest()->getParam('id');
?>

<div id="employee-form-container">
    <!-- ko scope: 'employeeForm' -->

    <h1 data-bind="text: pageTitle"></h1>

    <form data-bind="submit: saveEmployee">
        <input type="hidden" data-bind="value: employeeId"/>

        <fieldset class="fieldset">
            <div class="field required">
                <label class="label"><span><?= $escaper->escapeHtml(__('Name')) ?></span></label>
                <div class="control">
                    <input type="text" class="input-text" data-bind="value: name"/>
                </div>
            </div>

            <div class="field required">
                <label class="label"><span><?= $escaper->escapeHtml(__('Joining Date')) ?></span></label>
                <div class="control">
                    <input type="date" data-bind="value: joiningDate"/>
                </div>
            </div>

            <div class="field required">
                <label class="label"><span><?= $escaper->escapeHtml(__('Designation')) ?></span></label>
                <div class="control">
                    <input type="text" class="input-text" data-bind="value: designation"/>
                </div>
            </div>

            <div class="field">
                <label class="label"><span><?= $escaper->escapeHtml(__('Address')) ?></span></label>
                <div class="control">
                    <textarea data-bind="value: address"></textarea>
                </div>
            </div>

            <div class="field">
                <label class="label"><span><?= $escaper->escapeHtml(__('Status')) ?></span></label>
                <div class="control">
                    <select data-bind="value: status">
                        <option value="1"><?= $escaper->escapeHtml(__('Active')) ?></option>
                        <option value="0"><?= $escaper->escapeHtml(__('Inactive')) ?></option>
                    </select>
                </div>
            </div>

            <div class="field">
                <label class="label"><span><?= $escaper->escapeHtml(__('Hobbies')) ?></span></label>
                <div class="control">
                    <!-- ko foreach: hobbyOptions -->
                    <label style="margin-right: 12px;">
                        <input type="checkbox" data-bind="checked: $parent.hobbies, checkedValue: value"/>
                        <span data-bind="text: label"></span>
                    </label>
                    <!-- /ko -->
                </div>
            </div>
        </fieldset>

        <div class="actions-toolbar">
            <div class="primary">
                <button type="submit" class="action save primary">
                    <span><?= $escaper->escapeHtml(__('Save')) ?></span>
                </button>
            </div>
            <div class="secondary">
                <button type="button" class="action back" data-bind="click: goBack">
                    <span><?= $escaper->escapeHtml(__('Back')) ?></span>
                </button>
            </div>
        </div>
    </form>

    <!-- /ko -->
</div>

<script type="text/x-magento-init">
{
    "#employee-form-container": {
        "Magento_Ui/js/core/app": {
            "components": {
                "employeeForm": {
                    "component": "Adobe_Employee/js/employee-form",
                    "employeeId": "<?= (int)$id ?>",
                    "getUrl": "<?= $escaper->escapeJs($block->getUrl('adobeemployee/ajax/get')) ?>",
                    "saveUrl": "<?= $escaper->escapeJs($block->getUrl('adobeemployee/ajax/save')) ?>",
                    "backUrl": "<?= $escaper->escapeJs($block->getUrl('adobeemployee/employee/index')) ?>"
                }
            }
        }
    }
}
</script>